package server;

/**
 * Formatter that takes in LOGGER data and spits it to XML.
 * 
 * @author Kyle
 */
public class ServerLogFormatter {
	// path including the file
	private String path;

	/**
	 * Given a path, this formatter will receive logs from the logger and write
	 * them to the log file given in path.
	 * 
	 * @param path
	 *            XML file to write to, will be created if it doesn't exist.
	 */

}
